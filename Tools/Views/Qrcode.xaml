<Window x:Class="Tools.Views.Qrcode"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tools.Views"
        mc:Ignorable="d"
        Title="二维码" Height="200" Width="200" ResizeMode="NoResize"
        DataContext="{Binding QrcodeViewModel,Source={StaticResource Locator}}" Topmost="True" WindowStartupLocation="CenterScreen">
    <Grid>
        <Image Source="{Binding Image}" Margin="10" Stretch="Fill" />
    </Grid>
</Window>
